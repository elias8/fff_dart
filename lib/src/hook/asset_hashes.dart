/// Populated from elias8/fff_dart release binaries before publishing.
const String? releaseTag = null;

/// SHA-256 hashes keyed by `fff-*` release artifact name.
const assetHashes = <String, String>{};
